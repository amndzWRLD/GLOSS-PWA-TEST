export default function AvailabilityControl({ availability }) {
  return <div className="rounded-2xl border border-white/10 bg-zinc-950/70 p-4 space-y-3"><h3 className="font-semibold">Availability Control</h3><div className="flex items-center justify-between"><span className="text-zinc-400">Provider status</span><span className={`px-2 py-1 rounded-full text-xs ${availability.isOnline ? 'bg-emerald-500/20 text-emerald-300':'bg-zinc-700 text-zinc-200'}`}>{availability.isOnline ? 'Online':'Offline'}</span></div><div className="flex items-center justify-between"><span className="text-zinc-400">Incoming requests</span><span className={`px-2 py-1 rounded-full text-xs ${availability.paused ? 'bg-red-500/20 text-red-300':'bg-emerald-500/20 text-emerald-300'}`}>{availability.paused ? 'Paused':'Active'}</span></div><div className="text-sm text-zinc-300">Service radius: <strong>{availability.serviceRadiusKm}km</strong> · Capacity: <strong>{availability.activeSlots}/{availability.capacity}</strong></div><div className="space-y-1">{availability.workingHours.map((s)=><p key={s.day} className="text-xs text-zinc-400">{s.day}: {s.start} - {s.end}</p>)}</div></div>
}
